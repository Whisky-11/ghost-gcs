{
  "name": "ghost-gcs",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "pnpm": {
    "_comment": "Overrides are also mirrored in pnpm-workspace.yaml (pnpm 10's location) — keep both in sync until we bump pnpm.",
    "overrides": {
      "xml2js": "^0.6.2",
      "postcss": "^8.5.10"
    }
  }
}
